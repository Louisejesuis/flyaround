{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="mx-auto my-3">Planemodel creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" class="btn btn-primary" / >
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('planemodel_index') }}" class="btn btn-primary">Back to the list</a>
        </li>
    </ul>
{% endblock %}
